const svg = {
    book: require('./bookIcon.svg').default,
    search: require('./searchIcon.svg').default,
    setting: require('./settingIcon.svg').default,
    upload: require('./uploadIcon.svg').default,
}
export default svg;